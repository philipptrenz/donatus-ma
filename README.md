# Donatus Master Thesis

Jupyter Notebook to merge csv files from study into one document.

First, put all study files in `data/` folder, then execute `merge.ipynb`.

The merged data set is stored in `merged.csv`